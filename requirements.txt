# Core pipeline (Python 3.11+)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Optional, only for the StatcastMLB vendor integration
# (pip install -e vendor/StatcastMLB installs these itself):
# requests, pyarrow, duckdb, scipy, tqdm, rich
